namespace SourceFlow
{
    public static class EventExtensions
    {
        //public static TEvent Clone<TEvent>(this TEvent @event) where TEvent : IEvent, new()
        //{
        //    if (@event == null)
        //        throw new ArgumentNullException(nameof(@event));
        //    if (@event.Source == null)
        //        throw new ArgumentNullException(nameof(@event.Source));

        //    var newEvent = new TEvent
        //    {
        //        Source = @event.Source,
        //        SequenceNo = @event.SequenceNo + 1
        //    };

        //    return newEvent;
        //}
    }
}